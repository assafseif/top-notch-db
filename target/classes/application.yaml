spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=topnotchdb;encrypt=true;trustServerCertificate=true
    username: sa
    password: P@ssw0rd
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

server:
  port: 8080

# NOTE: For local development it is OK to set trustServerCertificate=true (accept self-signed certs).
# For production, import the SQL Server certificate into the JVM truststore and do NOT use trustServerCertificate=true.
